export class ListItem {
  id: string;
  title: string;
  caption: string;

  constructor(id: string, title: string, caption: string) {
    this.id = id;
    this.title = title;
    this.caption = caption;
  }
}
